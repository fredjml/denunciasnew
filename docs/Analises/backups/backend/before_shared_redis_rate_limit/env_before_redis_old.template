# Configuração do Servidor Backend

# Porta do servidor (padrão: 3000)
PORT=3000

# URL da API interna do MPT para recebimento das denúncias
# Substitua pela URL real fornecida pelo MPT
MPT_API_URL=https://api.interna.mpt.mp.br/denuncias

# Token de autenticação da API do MPT (se necessário)
MPT_API_TOKEN=seu_token_aqui

# Ambiente: development | production
NODE_ENV=development

# Origem pública exata do frontend. Obrigatória para CORS em produção.
FRONTEND_URL=https://denuncias.mpt.mp.br

# Documentação Swagger fica desabilitada em produção, salvo opt-in explícito.
ENABLE_API_DOCS=false

# Limite de submissões por endereço IP dentro da janela configurada.
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=20

# Saltos de proxy reverso confiáveis. Mantenha 0 sem proxy; normalmente 1 atrás de um único proxy.
TRUST_PROXY_HOPS=0

# Tamanho máximo de upload por arquivo (em bytes) - default: 20MB
MAX_FILE_SIZE=20971520

# Número máximo de arquivos por denúncia
MAX_FILES=10
